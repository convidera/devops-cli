version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/convidera/devops-cli

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
